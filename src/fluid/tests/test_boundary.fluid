
   glSelf = obj.find('self')

function testFunctionBoundary()
   local m = {}
   local clients = { }
   
   for i = 1, 2 do
      m[i] = {}
      for j = 1, 8 do
         table.insert(m[i], 'ISOLATED_CLIENT_' .. i .. '_MESSAGE_' .. j)
      end
   end

   local i = 1
   clients[1] = { }
   clients[1].messageIndex = 1
   print('messageIndex: ', clients[1].messageIndex)
   print(m[1][1])
   print(m[1][i])
   print(m[1][clients[1].messageIndex])
end

   local err, bc = glSelf.mtDebugLog('disasm')
   print(bc)

----------------------------------------------------------------------------------------------------------------------

   return {
      tests = { 'testFunctionBoundary' }
   }

-- Call integrated unit tests in the XPath module

   include 'xml'
   xq = mod.load('xquery')

-----------------------------------------------------------------------------------------------------------------------

function testUnitTests()
   assert(xq.UnitTest() == ERR_Okay, "One or more XPath unit tests failed")
end

return {
   tests = {
      'testUnitTests',
   }
}

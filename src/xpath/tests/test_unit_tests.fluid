-- Call integrated unit tests in the XPath module

   include 'xml'
   xp = mod.load('xpath')

-----------------------------------------------------------------------------------------------------------------------

function testUnitTests()
   assert(xp.UnitTest() == ERR_Okay, "One or more XPath unit tests failed")
end

return {
   tests = {
      'testUnitTests',
   }
}

--$FLUID:Include

module({ name="XRandR", copyright="Paul Manias © 2014-2017", version=1.0, prefix="xr" }, function()
  functionNames("xr",
    "SetDisplayMode",
    "Notify",
    "SelectInput",
    "GetDisplayTotal",
    "GetDisplayMode")

  struct("xrMode", { comment="Display mode." }, [[
    int Width    # Horizontal
    int Height   # Vertical
    int Depth    # bit depth
  ]])

end)

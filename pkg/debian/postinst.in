#!/bin/sh
# postinst script for kotuku
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <postinst> `configure' <most-recently-configured-version>
#        * <old-postinst> `abort-upgrade' <new version>
#        * <conflictor's-postinst> `abort-remove' `in-favour' <package>
#          <new-version>
#        * <postinst> `abort-remove'
#        * <deconfigured's-postinst> `abort-deconfigure' `in-favour'
#          <failed-install-package> <version> `removing'
#          <conflicting-package> <version>
# for details, see https://www.debian.org/doc/debian-policy/ or
# the debian-policy package

case "$1" in
    configure)
        # Update dynamic linker cache for any shared libraries
        if command -v ldconfig >/dev/null 2>&1; then
            ldconfig
        fi

        # Create symlinks or register with alternatives if needed
        # (This would be customized based on Kotuku's specific needs)

        # Set up any necessary permissions on config directories
        if [ -d /usr/share/kotuku/config ]; then
            chmod -R 644 /usr/share/kotuku/config
            find /usr/share/kotuku/config -type d -exec chmod 755 {} \;
        fi

        # Make scripts executable if they exist
        if [ -d /usr/share/kotuku/scripts ]; then
            find /usr/share/kotuku/scripts -name "*.fluid" -exec chmod 644 {} \;
            find /usr/share/kotuku/scripts -type d -exec chmod 755 {} \;
        fi
        ;;

    abort-upgrade|abort-remove|abort-deconfigure)
        ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
        ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0